<UserControl x:Class="PeliculasStudio.Vistas.UC_Inicio"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PeliculasStudio.Vistas"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             >
    <Grid x:Name="GridPrincipal">
        <Grid.Background>
            <RadialGradientBrush Center="0.5,0.4" RadiusX="1.5" RadiusY="1.5">
                <GradientStop Color="{DynamicResource ColorFocoCentro}" Offset="0"/>
                <GradientStop Color="{DynamicResource ColorBasePantalla}" Offset="1"/>
            </RadialGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="20,10" CornerRadius="20" Background="{DynamicResource BrushFormulario}">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.1" ShadowDepth="2"/>
            </Border.Effect>
            <Grid Margin="15,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Width="45" Height="45" Cursor="Hand" Click="BtnPerfil_Click">
                    <Button.Template>
                        <ControlTemplate>
                            <Border CornerRadius="25" Background="{DynamicResource BrushInput}" BorderBrush="{DynamicResource BrushAccionResaltado}" BorderThickness="2">
                                <TextBlock Text="👤" Foreground="{DynamicResource BrushTextoPrincipal}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="👤 Mi Perfil" Click="MenuPerfil_Click"/>
                            <Separator/>
                            <MenuItem Header="🚪 Cerrar Sesión" Click="MenuCerrarSesion_Click" Foreground="Red"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>

                <Button Grid.Column="1" 
        Content="🎬 Catálogo" 
        Margin="20,0,0,0" 
        Height="40" 
        Padding="20,0"
         Foreground="{DynamicResource BrushTextoPrincipal}"
        Background="Transparent"
        BorderBrush="Transparent"
        Click="BtnIrCatalogo_Click" 
        FontWeight="Bold"
        Cursor="Hand">

                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="1.5"
                    CornerRadius="16">

                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource BrushInput}"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource BrushAccionResaltado}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource BrushTextoPrincipal}"/>
                                </Trigger>

                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource BrushSeparador}"/>
                                    <Setter Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>

                    <Button.RenderTransformOrigin>0.5,0.5</Button.RenderTransformOrigin>
                    <Button.RenderTransform>
                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                    </Button.RenderTransform>
                </Button>

                <Border Grid.Column="2" Margin="30,10" CornerRadius="15" Background="{DynamicResource BrushInput}">
                    <Grid>
                        <TextBlock Text="🔍 Buscar película..." VerticalAlignment="Center" Margin="15,0" Foreground="{DynamicResource BrushTextoAyuda}" IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=txtBuscador}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBox x:Name="txtBuscador" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="15,0" FontSize="14" Foreground="{DynamicResource BrushTextoPrincipal}"/>
                    </Grid>
                </Border>
                <ToggleButton x:Name="btnTema" Grid.Column="3" Width="100" Height="45" Cursor="Hand" Click="btnTema_Click">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="border" Background="Transparent" BorderBrush="{DynamicResource BrushAccionResaltado}" BorderThickness="1.5" CornerRadius="22">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="TEMA" FontSize="11" FontWeight="ExtraBold" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource BrushAccionResaltado}"/>
                                    <TextBlock x:Name="txtIcon" Text="☀" FontSize="20" VerticalAlignment="Center" Foreground="{DynamicResource BrushAccionResaltado}"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="txtIcon" Property="Text" Value="☾"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" Margin="0,10,0,0" VerticalScrollBarVisibility="Hidden">
            <StackPanel Margin="20,0">

                <Border Height="400" CornerRadius="20" Margin="0,0,0,30" ClipToBounds="True" Background="Black">
                    <Grid>
                        <Image x:Name="imgBannerFondo" Source="{Binding ElementName=imgBanner, Path=Source}" Stretch="UniformToFill" Opacity="0.6">
                            <Image.Effect>
                                <BlurEffect Radius="60"/>
                            </Image.Effect>
                        </Image>
                        <Rectangle Fill="#99000000"/>
                        <Grid Margin="40">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                                <Border Background="#E50914" HorizontalAlignment="Left" CornerRadius="4" Padding="12,6" Margin="0,0,0,15">
                                    <TextBlock Text="DESTACADO HOY" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                </Border>
                                <TextBlock x:Name="txtBannerTitulo" Text="TITULO" FontSize="42" FontWeight="Black" Foreground="White" TextWrapping="Wrap" Margin="0,0,0,30"/>
                                <Button Content="▶ REPRODUCIR" Width="200" Height="55" HorizontalAlignment="Left" Background="White" Foreground="Black" FontWeight="ExtraBold" Click="BtnBanner_Click" Cursor="Hand">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="28"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                            <Border Grid.Column="1" CornerRadius="12" ClipToBounds="True">
                                <Image x:Name="imgBanner" Stretch="Uniform" MaxHeight="320"/>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>

                <TextBlock x:Name="txtSeguirViendo" Text="Seguir Viendo" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource BrushTextoPrincipal}" Margin="10,0,0,15" Visibility="Collapsed"/>

                <ScrollViewer x:Name="listaSeguirViendoScrollViewer" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled" PanningMode="HorizontalOnly" Margin="0,0,0,30" Visibility="Collapsed">
                    <ItemsControl x:Name="listaSeguirViendo">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="280" Height="140" Margin="10" CornerRadius="12" Background="{DynamicResource BrushFormulario}" Cursor="Hand" MouseUp="Card_MouseUp">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" CornerRadius="12,0,0,12" ClipToBounds="True">
                                            <Image Source="{Binding RutaImagenCompleta}" Stretch="UniformToFill"/>
                                        </Border>
                                        <Border Grid.Column="0" Background="#66000000" CornerRadius="12,0,0,12">
                                            <TextBlock Text="▶" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24"/>
                                        </Border>

                                        <StackPanel Grid.Column="1" Margin="10" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Titulo}" FontWeight="Bold" Foreground="{DynamicResource BrushTextoPrincipal}" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="Reanudar" Foreground="{DynamicResource BrushAccionResaltado}" FontSize="12" Margin="0,5,0,0"/>
                                            <Border Height="3" Background="#333" Margin="0,10,0,0" CornerRadius="1">
                                                <Border HorizontalAlignment="Left" Width="60" Background="{DynamicResource BrushAccionResaltado}" CornerRadius="1"/>
                                            </Border>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <TextBlock Text="Recomendados para ti" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource BrushTextoPrincipal}" Margin="10,0,0,15"/>

                <ScrollViewer x:Name="listaDestacadosScrollViewer" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled" PanningMode="HorizontalOnly" Margin="0,0,0,40">
                    <ItemsControl x:Name="listaDestacados">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="160" Height="240" Margin="10" CornerRadius="15" Background="{DynamicResource BrushFormulario}" Cursor="Hand" MouseUp="Card_MouseUp">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="10" Opacity="0.15" ShadowDepth="4"/>
                                    </Border.Effect>
                                    <Grid>
                                        <Border CornerRadius="15" ClipToBounds="True">
                                            <Image Source="{Binding RutaImagenCompleta}" Stretch="UniformToFill"/>
                                        </Border>
                                        <Border VerticalAlignment="Bottom" Height="60" CornerRadius="0,0,15,15" Background="#CC000000">
                                            <TextBlock Text="{Binding Titulo}" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" Margin="5"/>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Rectangle Height="50"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
